server:
  port: 8088
spring:
  application:
    name: api-gateway
  #gateway配置
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true #是否启用自动识别nacos服务

    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos

